/**
 * Table Schema: RequestForm
 * ID           auto
 * Title        string
 * Content      string
 * Status       string(enum of status: waiting, cancled, checked, accepted)
 * AceptedBy    string(=username)
 * CreatedBy    string(=username)
 * CreatedAt    date
 */